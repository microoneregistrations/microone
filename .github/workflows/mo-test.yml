name: mo-test
on:
  workflow_dispatch:
    inputs:
      note:
        description: free text, echoed below
        required: false
        default: hello
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.note }}"
